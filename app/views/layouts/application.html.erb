<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>
  <body>
    <!-- headerのパーションをレンダリング -->
    <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <!-- footerのパーシャルをレンダリング -->
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
